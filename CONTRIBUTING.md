# コントリビュートガイド

ReactiveExtream への貢献を歓迎します。**一番うれしいのはステージの追加**です。JS をほとんど書かずに、データを 1 つ足すだけで新しい問題が増えます。

- 環境構築: 不要（ビルドもパッケージマネージャもありません）
- 動かし方: `prototype/index.html` をブラウザで開くだけ
- 言語: Issue / PR は日本語でも英語でも構いません

---

## 何を歓迎するか

| 種類 | 歓迎度 | 備考 |
| --- | --- | --- |
| 新しいステージ（実務パターンの題材） | ★★★ | 一番ほしいもの。下の手順を参照 |
| オペレータの意味論のバグ報告 | ★★★ | 「RxJS ではこうなるのに」という指摘は再現手順付きで |
| 既存ステージの `goal` / `insight` / ヒントの改善 | ★★ | 言い回しの改善も歓迎 |
| 誤字・リンク切れ | ★★ | 気軽に PR してください |
| 英語 UI 対応 | ★★ | 大きい話なので先に Issue で相談 |
| 新オペレータの追加 | ★ | 意味論の設計が必要。先に Issue で相談 |
| ビルドツール・フレームワークの導入 | ✕ | 「開いたら動く」を壊さないことが本作の設計方針です |

---

## ステージを追加する

ステージ定義は `prototype/app.js` の `STAGES` 配列にあります。**純粋なデータ**なので、そのまま JSON に出せる形を保ってください。

### 1. `STAGES` に 1 件足す

```js
{
  id: "stage-95",                       // 一意。既存の最大値 + 1
  title: "Stage 95: 二重送信の抑止",     // ステージ選択に出る名前
  goal: "連打されたクリックを、最初の 1 回だけ通そう。",  // 画面上部の指示文
  duration: 12,                          // タイムラインの長さ（目盛り数）
  input: {
    events: [{ t: 1, value: 1 }, { t: 2, value: 1 }, { t: 3, value: 1 }],
    completeAt: 11,
  },
  expected: {                            // 正解ストリーム
    events: [{ t: 1, value: 1 }],
    completeAt: 11,
  },
  availableOperators: ["throttleTime", "debounceTime", "take"], // 選択肢
  maxNodes: 2,                           // 置けるパーツ数の上限（A + B 合計）
  hints: ["連打の『最初』を通すか『最後』を通すか。"],  // 省略可（ヒント1段目を上書き）
  insight: "ボタンの二重送信防止は throttleTime が定番です。",  // 省略可・クリア時に表示
}
```

2 入力ステージにする場合は `inputB: { events, completeAt }` を足します。`inputLabel` / `inputBLabel` にシグナル名（例: `"保存クリック"`）を入れると、入力レーンの見出しがその名前になります。

### 2. `SOLUTIONS` に正準解を足す

3 段階ヒントはここから自動生成されます。**必須**です。

```js
"stage-95": { a: [sol("throttleTime", { d: 5 })] },
// 2 入力ステージで B 側にもパーツを置く場合:
// "stage-96": { a: [sol("zip", { op: "add" })], b: [sol("skip", { count: 1 })] },
```

### 3. サイクルのサイズを合わせる

ファイル先頭の `CYCLES` にある該当サイクルの `size` を +1 してください。`STAGES` の総数と `CYCLES` の `size` の合計は必ず一致します。

### 4. 動作を確認する

```sh
node --check prototype/app.js
python3 -m http.server 8000 --directory prototype
```

ブラウザでステージを開き、次を確認してください。

- [ ] `expected` が本当に到達可能である（自分で解けること）
- [ ] `SOLUTIONS` の正準解をそのまま置くとクリアになる
- [ ] ヒント 1 → 2 → 3 が破綻していない
- [ ] `maxNodes` が正準解の個数以上になっている
- [ ] `availableOperators` に、そのサイクルまでに未登場のオペレータが混ざっていない

---

## 良いステージの条件

このゲームのレベルデザインには方針があります。踏襲していただけると助かります。

1. **1 ステージ = 1 つの気づき。** 「このオペレータのここが分かっていないと解けない」が 1 つに絞れていること。
2. **導入は中立値から。** 新しいオペレータを初めて出すステージでは、追加直後のパラメータが**ストリームを変えない中立値**（`map: ×1`、`filter: ≥0`、時間系: `d=0` 等）になるようにします。プレイヤーが「置いても何も起きない → パラメータを触る」と自然に学べるためです。
3. **選択肢が答えにならないこと。** 導入ステージでは `availableOperators` を絞って「正しいオペレータを選ぶ」課題にします。ただし選択肢は必ず 2 つ以上にしてください（1 つだと考える余地がありません）。
4. **実務の題材が強い。** 「type-ahead」「保存ボタン」「二重配信の排除」のように、現場で見たことのある名前が付くステージは記憶に残ります。`insight` にその一言を書いてください。
5. **別解を潰さない。** 判定は出力ストリームだけを見ます。`maxNodes` を厳しくしすぎると別解が死にます。正準解 +1 を目安に。
6. **同時刻に 2 つのイベントを置かない。** v0 の制約です（差分表示が曖昧になるため）。

---

## オペレータの意味論を直す

`OPERATOR_DEFS` の各エントリは `(stream, params) => stream` の純粋関数です。変更する場合:

1. **`docs/implementation-plan.md` の「v0 確定仕様」も必ず一緒に更新してください。** 仕様書が正で、実装がそれに従う建て付けです。
2. 既存 94 ステージの `expected` が壊れないことを確認してください（意味論の変更は既存ステージのクリア可否に波及します）。
3. RxJS と意図的に異なる挙動には、`RX_NOTES` に「本物はこう違う」の注記を足してください。クリア時のコード生成に表示されます。

---

## PR の出し方

1. ブランチを切る（`stage/xxx`、`fix/xxx` など）
2. `node --check prototype/app.js` が通ることを確認
3. PR の説明に以下を書く
   - 何のステージ／修正か
   - 追加ステージなら「学ばせたい気づき」を 1 行で
   - 意味論を変えた場合は、RxJS の該当ドキュメントへのリンク

CI はありません。レビューは手動で、実際に遊んで確認します。

---

## ライセンスについて

PR を送った時点で、以下のライセンスでの公開に同意したものとみなします。

- コードの変更 → MIT
- ステージ定義・文書の追加 → CC BY 4.0

詳細は [LICENSE](LICENSE) / [LICENSE-CONTENT](LICENSE-CONTENT) を参照してください。
