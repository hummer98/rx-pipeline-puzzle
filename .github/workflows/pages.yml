name: Deploy to GitHub Pages

# prototype/ をサイトのルート、docs/ を /docs/ として公開する。
# ビルドは無し（静的ファイルをそのまま並べ替えるだけ）。
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# 公開中のデプロイは中断せず、後続を1つだけ待たせる
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: 構文チェック
        run: |
          for f in prototype/*.js; do node --check "$f"; done

      - name: サイトを組み立てる
        run: |
          mkdir -p _site
          cp -R prototype/. _site/
          mkdir -p _site/docs
          cp -R docs/. _site/docs/
          # 英語ページ（/en/）を生成する。head だけ英語に差し替えた薄いコピーで、
          # CSS と JS は 1 つ上を参照するため資産は二重にならない。
          node tools/build-en.mjs _site
          # 資産 URL にコミット SHA を付ける。Pages の max-age=600 で
          # 新旧の JS が混ざるのを防ぐ。
          node tools/stamp-assets.mjs _site "${GITHUB_SHA::7}"
          # Jekyll の処理を無効化（_ で始まるファイルが消えるのを防ぐ）
          touch _site/.nojekyll

      - uses: actions/upload-pages-artifact@v5
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
